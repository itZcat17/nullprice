# .NET
bin/
obj/
*.user
.vs/
.idea/
TestResults/

# Node / Electron
node_modules/
store/dist/

# Built releases served by the local test feed — large binaries, rebuildable
store/feed/*.exe
store/feed/*.pdb

# Published app binaries and archives — rebuildable from source, hundreds of MB
apps/**/dist/
apps/**/portable/
apps/**/*.exe
apps/**/*.zip

# OS
Thumbs.db
desktop.ini
